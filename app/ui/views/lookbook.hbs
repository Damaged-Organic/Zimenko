<!DOCTYPE html>
<html lang="en">
<head>
    <script async src="https://www.googletagmanager.com/gtag/js?id=UA-106471938-1"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments)};
      gtag('js', new Date());

      gtag('config', 'UA-106471938-1');
    </script>
    {{> head/meta meta=meta}}
	{{> head/favicons}}
    <link rel="stylesheet" href="{{static 'css' 'lookbook.bundle.min.css'}}">
</head>
<body class="preload">
    <div id="page">
        <span id="top-lane"></span>
        <div id="menu">
            <div class="top-holder">
                {{> header/logo}}
                {{> header/btn_close}}
                {{> header/langs_holder}}
            </div>
            {{> menu/menu_holder}}
			{{> menu/socials_holder}}
			{{> menu/text_overlay}}
        </div>
        <main id="content">
            <header id="header">
                {{> header/logo_holder}}
                {{> header/open_menu_button}}
                {{> header/langs_holder}}
            </header>
            <section class="container lookbook-holder">
                {{> common/categories_holder lookbookCategories=lookbookCategories}}
                <div class="inner" id="gallery-holder">
                    <div class="lookbook" id="gallery-list">
                        {{#each lookbooks}}
                            <article class="album-holder gallery-item">
                                <h2>
                                    <span class="style">{{ style }}</span>
                                    <span class="line"></span>
                                    <span class="title">{{ title }}</span>
                                </h2>
                                <div class="album">
                                    <div class="inner">
                                        <a href="{{url 'lookbookAlbum' datestamp=datestamp slug=slug}}">
                                            <figure>
                                                <img src="{{ cloudinary images.[0].public_id '{
                                                    "format": "png", "width": 840}'
                                                }}" alt="{{ title }}">
                                            </figure>
                                            <span class="more">{{i18n 'view album'}}</span>
                                        </a>
                                    </div>
                                    <span class="number-holder">
                                        {{#blockRelativeIndex @root.model @index}}
                                          {{#each this}}
                                            <span class="num">{{ this }}</span>
                                          {{/each}}
                                        {{/blockRelativeIndex}}
                                    </span>
                                </div>
                            </article>
                        {{/each}}
                    </div>
                </div>
                <span class="tooltip-holder">{{i18n 'scroll to discover more'}}</span>
                {{> common/navigation}}
            </section>
        </main>
        {{> loaders/progress_holder}}
        <div id="common-loader-holder">
            {{> loaders/loader}}
        </div>
        {{> common/landscape}}
    </div>
    <script src="{{static 'js' 'lookbook.bundle.min.js'}}"></script>
</body>
</html>
